-- s_clue_cst 客户数据
select
    1 as `authenticated`,
    null as `id_card_type`,
    null as `id_card_no`,
    cst_name as `name`,
    gender as `gender`,
    null as `birthday`,
    null as `birth_place`,
    null as `education`,
    null as `work_area`,
    null as `home_area`,
    null as `industry`,
    now() as `create_time`,
    now() as `update_time`,
    '系统数据迁移' as `create_person`,
    '系统数据迁移' as `update_person`,
    0 as `deleted`,
    uuid as `account_id`,
    1000 as `app_id`,
    null as `open_id`,
    cst_tel as `mobile`,
    cst_name as `nickname`,
    null as `profile_picture`,
    null as `signature`,
    null as `mip`,
    null as `password`,
    1 as `state`,
    null as `extra_params`,
    uuid as `uid`,
    null as `authentication_id`,
    null as `channel_type`,
    null as `channel_name`,
    null as `qrcode_template_id`,
    's_clue_cst' as `register_channel`,
    null as `start_time`,
    null as `end_time`,
    null as `union_id`,
    null as `client_id`,
    null as `examine_status`,
    null as `examine_reason`,
    null as `id_card_suffix`,
    null as `erp_user_id`,
    null as `erp_username`,
    null as `APP_ROLE_MCRM`,
    null as `APP_ROLE_ZITUO`
from s_clue_cst
union all
-- s_clue_related_marketing 客户数据
select
    1 as `authenticated`,
    null as `id_card_type`,
    null as `id_card_no`,
    cst_name as `name`,
    (case when `gender` = '男' then 1 else 0 end) as `gender`,
    null as `birthday`,
    null as `birth_place`,
    null as `education`,
    null as `work_area`,
    null as `home_area`,
    null as `industry`,
    now() as `create_time`,
    now() as `update_time`,
    '系统数据迁移' as `create_person`,
    '系统数据迁移' as `update_person`,
    0 as `deleted`,
    clue_id as `account_id`,
    1002 as `app_id`,
    null as `open_id`,
    cst_tel as `mobile`,
    cst_name as `nickname`,
    null as `profile_picture`,
    null as `signature`,
    null as `mip`,
    null as `password`,
    1 as `state`,
    null as `extra_params`,
    clue_id as `uid`,
    null as `authentication_id`,
    null as `channel_type`,
    null as `channel_name`,
    null as `qrcode_template_id`,
    's_clue_related_marketing' as `register_channel`,
    null as `start_time`,
    null as `end_time`,
    null as `union_id`,
    null as `client_id`,
    null as `examine_status`,
    null as `examine_reason`,
    null as `id_card_suffix`,
    null as `erp_user_id`,
    null as `erp_username`,
    null as `APP_ROLE_MCRM`,
    null as `APP_ROLE_ZITUO`
from s_clue_related_marketing
union all
-- s_customer 客户数据
select
    1 as `authenticated`,
    null as `id_card_type`,
    null as `id_card_no`,
    cst_name as `name`,
    CASE
    WHEN `gender` = 0 THEN '1'
    WHEN `gender` = 1 THEN '0'
    WHEN `gender` = 2 THEN '1'
    WHEN `gender` = '男' THEN '1'
    WHEN `gender` = '女' THEN '0'
    ELSE '1'
    END AS `gender`,
    null as `birthday`,
    null as `birth_place`,
    null as `education`,
    null as `work_area`,
    null as `home_area`,
    null as `industry`,
    now() as `create_time`,
    now() as `update_time`,
    '系统数据迁移' as `create_person`,
    '系统数据迁移' as `update_person`,
    0 as `deleted`,
    s_customerId as `account_id`,
    1004 as `app_id`,
    null as `open_id`,
    mobile_tel as `mobile`,
    cst_name as `nickname`,
    null as `profile_picture`,
    null as `signature`,
    null as `mip`,
    null as `password`,
    1 as `state`,
    null as `extra_params`,
    s_customerId as `uid`,
    null as `authentication_id`,
    null as `channel_type`,
    null as `channel_name`,
    null as `qrcode_template_id`,
    's_customer' as `register_channel`,
    null as `start_time`,
    null as `end_time`,
    null as `union_id`,
    null as `client_id`,
    null as `examine_status`,
    null as `examine_reason`,
    null as `id_card_suffix`,
    null as `erp_user_id`,
    null as `erp_username`,
    null as `APP_ROLE_MCRM`,
    null as `APP_ROLE_ZITUO`
from s_customer;
union all
select
    1 as `authenticated`,
    null as `id_card_type`,
    null as `id_card_no`,
    cst_name as `name`,
    case
        when gender = '男' then 1
        else 0
    end as `gender`,
    null as `birthday`,
    null as `birth_place`,
    null as `education`,
    null as `work_area`,
    null as `home_area`,
    null as `industry`,
    now() as `create_time`,
    now() as `update_time`,
    '系统数据迁移' as `create_person`,
    '系统数据迁移' as `update_person`,
    0 as `deleted`,
    uuid as `account_id`,
    1003 as `app_id`,
    null as `open_id`,
    cst_tel as `mobile`,
    cst_name as `nickname`,
    null as `profile_picture`,
    null as `signature`,
    null as `mip`,
    null as `password`,
    1 as `state`,
    null as `extra_params`,
    uuid as `uid`,
    null as `authentication_id`,
    null as `channel_type`,
    null as `channel_name`,
    null as `qrcode_template_id`,
    'm_member_cst' as `register_channel`,
    null as `start_time`,
    null as `end_time`,
    null as `union_id`,
    null as `client_id`,
    null as `examine_status`,
    null as `examine_reason`,
    null as `id_card_suffix`,
    null as `erp_user_id`,
    null as `erp_username`,
    null as `APP_ROLE_MCRM`,
    null as `APP_ROLE_ZITUO`
from m_member_cst
union all
select
    1 as `authenticated`,
    null as `id_card_type`,
    null as `id_card_no`,
    cst_name as `name`,
    case
        when gender = 1 then 1
        else 0
    end as `gender`,
    null as `birthday`,
    null as `birth_place`,
    null as `education`,
    null as `work_area`,
    null as `home_area`,
    null as `industry`,
    now() as `create_time`,
    now() as `update_time`,
    '系统数据迁移' as `create_person`,
    '系统数据迁移' as `update_person`,
    0 as `deleted`,
    b_regbroker_cstId as `account_id`,
    1001 as `app_id`,
    null as `open_id`,
    mobile_tel as `mobile`,
    cst_name as `nickname`,
    null as `profile_picture`,
    null as `signature`,
    null as `mip`,
    null as `password`,
    1 as `state`,
    null as `extra_params`,
    b_regbroker_cstId as `uid`,
    null as `authentication_id`,
    null as `channel_type`,
    null as `channel_name`,
    null as `qrcode_template_id`,
    'b_regbroker_cst' as `register_channel`,
    null as `start_time`,
    null as `end_time`,
    null as `union_id`,
    null as `client_id`,
    null as `examine_status`,
    null as `examine_reason`,
    null as `id_card_suffix`,
    null as `erp_user_id`,
    null as `erp_username`,
    null as `APP_ROLE_MCRM`,
    null as `APP_ROLE_ZITUO`
from b_regbroker_cst
